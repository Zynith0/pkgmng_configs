# Wtf is this?
As you may be asking, wtf is this? This is a repo to store my config for an unreleased project called "pkgmng" (subject to change)
It's essentially a declarative "frontend" for your system level package manager that also includes things like copying config files to your .config folder,
and downloading and compiling packages from source
